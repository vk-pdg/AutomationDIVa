echo "---build start---"

stage('Checkout Stage') {
    echo "---Checkout---"
}

stage('Build Stage') {
    echo "---Build Stage---"
        docker container ls
}

stage('Push Stage') {
    echo "---Push Stage---"
}